spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/grsmnk/spring-cloud-bus-test
          force-pull: true
          username:
          password:
    bus:
      enabled: true
      trace:
        enabled: true